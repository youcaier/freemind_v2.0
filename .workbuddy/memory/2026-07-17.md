# 2026-07-17 工作日志

- 修复部分节点无法双击进入编辑状态
  - 问题：用户反馈有些节点双击无法编辑，右键菜单编辑正常
  - 根因：第一次点击节点后触发 `onSelect`，导致 `selectedId` 状态变化，React 重新渲染后 DOM 元素被替换，浏览器无法将两次点击识别为同一 HTML 元素的 `dblclick`
  - 修复：
    - 修改 `src/components/MindMapCanvas.tsx`：
      - 新增 `lastMouseDownRef`，记录最近一次 `mousedown` 的节点 ID 和时间戳
      - 在 `handleNodeMouseDown` 中检测：300ms 内同一节点连续两次按下，即调用 `onStartEdit` 进入编辑
      - 检测到双击时清空 `dragStartRef`，避免第二次按下被当成拖拽起点
      - 保留移除 `e.preventDefault()` 和节点 `user-select: none` 的改动
  - 验证：
    - TypeScript 类型检查通过
    - Vite 生产构建成功
    - Tauri release 构建 `.app` 成功（`.dmg` 仍因沙箱环境 `bundle_dmg.sh` 调用 Finder 失败，使用 `hdiutil` 手动创建）
    - 应用已重新启动待用户验证
    - 代码提交 `d646ca2` 并推送到 GitHub `https://github.com/youcaier/freemind_v2.0`
    - 本地 dmg 安装包已更新：`freemind_v20_0.1.0_aarch64.dmg`（约 2.5MB）
