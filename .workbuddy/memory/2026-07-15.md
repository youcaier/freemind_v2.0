# 2026-07-15 工作日志

- 梳理并生成 FreeMind v2.0 项目进度报告：项目进度报告.md
- 技术栈：Tauri 2 + React 19 + TypeScript + Vite
- 当前为 MVP 阶段：核心脑图编辑、4 种布局、4 种连线、样式、撤销重做、文件持久化、快捷键均已实现
- TypeScript 类型检查通过
- 修复画布居中：MindMapCanvas 改用 forwardRef 暴露 centerView，按根节点居中，切换布局时自动居中，工具栏新增“居中”按钮
- 修复节点文字截断：layout-engine/index.ts 中 measureNode 对中文字符按字宽估算，padding 与 CSS 统一为 12px
- 修复多选 Tab 添加子节点无效：useMindMap 新增 addChildNodes 支持批量添加，App.tsx 中 Tab 快捷键对多选生效，添加后自动选中新节点
- 修复 Tauri 桌面应用中 Tab 等快捷键无响应：App.tsx 在 document capture phase 阻止 WebView 默认行为；MindMapCanvas 在 mount 时自动 focus 画布容器
- 修复部分节点 Tab 添加子节点无反应：useMindMap 的 addChildNodes 把节点添加逻辑放到 setData updater 内部，避免 React 批处理导致状态错乱；Playwright 自动化测试验证根/子/孙节点 Tab 均正常
- 调整添加子节点后的选中行为：addChildNodes 不再把选中状态切换到新节点，保持原选中节点不变，与之前行为一致
- 修复部分子节点鼠标左键无法选中：MindMapCanvas 中区分点击与拖拽，mousedown 仅记录候选拖拽，移动超过 3px 阈值才真正进入拖拽；拖拽期间节点保持 pointerEvents: auto，避免状态卡住导致后续点击失效；浏览器/Playwright 验证点击与拖拽均正常
- 修复左右平衡布局节点重叠：layout-engine/layouts/balanced.ts 中 layoutDirectionalSubtree 子节点堆叠从 startY 开始，并用 box.totalHeight 计算子树根节点纵坐标，使父节点垂直居中于子节点堆叠；Playwright 截图验证无重叠
- 修复鱼骨图布局节点重叠：layout-engine/layouts/fishbone.ts 中 layoutRightSubtree 增加 expandDirection 参数，让上侧分支子节点向上展开、下侧分支子节点向下展开；修正子树 totalHeight 计算；layoutFishboneSide 中一级分支间距基于子树总高度；Playwright 34 节点测试 0 重叠
- 修复各布局下连线端点选择不合理：MindMapCanvas 中 connectionAnchors 增加 layout 参数；组织结构图统一垂直连接；时间轴布局仅在子节点位于父节点正下方时使用垂直连接，其他情况使用水平连接；平衡树、鱼骨图统一水平连接；Playwright 验证 4 种布局 x 4 种连线风格均正常
- 修复组织结构图连线样式切换无明显效果：org 布局在 orthogonal/rounded 时保持“垂直树”结构，在 bezier/straight 时改用通用 connectionAnchors + buildConnectionPath，让曲线/直线样式可见；TypeScript 类型检查通过；应用已重新启动待验证
- 修复双击无法编辑节点：NodeView 增加 onStartEdit 回调与 onDoubleClick 事件处理，双击节点直接进入编辑状态；编辑后通过 Enter、Escape、失去焦点均可调用 onCommitEdit 保存；TypeScript 类型检查通过；应用已重新启动待验证
- 修复搜索后添加子节点父节点选择错乱：App.tsx 中搜索 useEffect 仅在 keyword 变化时重置 searchIndex；data 变化（添加节点等）只更新 matches 列表不重置索引；第二个 useEffect 只监听 searchIndex/searchKeyword，避免 data 变化导致自动跳回第一个匹配节点；TypeScript 类型检查通过；应用已重新启动待验证
- 实现画布缩放控制：MindMapCanvas 暴露 zoomIn/zoomOut/resetZoom 方法，以画布中心为锚点缩放，范围 20%~400%；通过 onScaleChange 回调实时同步缩放百分比到 App.tsx 工具栏；工具栏新增 - / + / 100% 按钮和百分比显示；TypeScript 类型检查通过；应用已重新启动待验证
