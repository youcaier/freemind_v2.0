# FreeMind v2.0 交互问题排查总结

## 问题链

本次修复的不是单一 bug，而是一连串由“节点交互事件处理”引发的连锁问题，按时间顺序如下：

| 轮次 | 用户反馈 | 根因 | 修复点 |
|---|---|---|---|
| 1 | 多选状态下按 `Tab` 添加子节点没反应 | 快捷键只认 `selectedId`，多选时 `selectedId` 为空 | `useMindMap` 新增 `addChildNodes`；`App.tsx` 让 `Tab` 对 `selectedIds` 生效 |
| 2 | `Tab` 完全失效，右键菜单还能加 | Tauri/macOS WKWebView 会拦截 `Tab`/`Space`/`Delete` 等键，触发系统提示音或焦点切换 | `App.tsx` 在 `document` 捕获阶段 `preventDefault`；`MindMapCanvas` mount 时自动 `focus` |
| 3 | 部分节点按 `Tab` 没反应 | `addChildNodes` 把 `setData` 和 `setSelectedId` 分开写，`newIds` 在 React 批处理外读取，时机错误 | 把添加逻辑全部放到 `setData` updater 内部 |
| 4 | 添加子节点后选中自动跳到新节点 | 修复轮次 3 时顺手把“选中新节点”写进了逻辑 | `addChildNodes` 添加后不再切换选中 |
| 5 | 左键点不中子节点，右键可以 | `handleNodeMouseDown` 把每次左键点击都当成拖拽启动，立即 `setDragging` 并渲染 `pointerEvents: none`；在 Tauri WebView 里 click/mouseup 事件序列被打断，导致部分节点卡住或选不中 | 引入拖拽阈值（>3px 才真正拖拽），普通点击不再进入拖拽；拖拽时保持 `pointerEvents: auto` |

## 核心根因

本次所有问题的共同根因是 **事件处理没有区分“点击”和“拖拽”**：

```tsx
// 原代码：mousedown 立即 setDragging
const handleNodeMouseDown = (nodeId, e) => {
  onSelect(nodeId, 'replace');
  setDragging({ nodeId, offsetX: 0, offsetY: 0, ... }); // ← 问题在这里
};
```

这带来三个隐患：

1. **click 事件被挤掉**：mousedown 后立即 `setDragging` → 节点渲染 `pointerEvents: none` → 后续的 `click`/`mouseup` 目标发生变化。在桌面 WebView 里表现尤其明显，部分节点看起来点不动。
2. **状态容易卡住**：如果某次拖拽的 `mouseup` 没正常触发（鼠标移出窗口、WebView 丢事件等），`dragging` 状态无法清理，对应节点长期处于 `pointerEvents: none`。
3. **每次点击都会走拖拽结束逻辑**：即使只是轻微点击，也会调用 `onMoveNode` / `onReorderNode`，引发不必要的布局重算，增加出错面。

## 为什么多次修复才定位

1. **症状和根因相隔较远**：用户先说 Tab 失效，后说选不中。表面上像是快捷键、选中逻辑、拖拽逻辑各自独立的问题，实际上都源自事件处理模型不合理。
2. **浏览器里复现不了**：Chromium 对事件序列的容忍度更高，Playwright 自动化测试一直通过。问题只在 Tauri/WKWebView 这种更接近原生 OS 事件模型的环境里才暴露。
3. **修 A 引出 B**：每次只针对当前症状修，比如把多选 Tab 修好后，又暴露出拖拽导致的事件问题；把 React 批处理修好后，又暴露出选中切换行为被改的问题。
4. **缺少拖拽阈值设计**：最早实现时默认“按下即拖拽”，没有区分 click/drag 的防御性设计。这是交互类功能常见的设计债务。

## 最终修复方案

`src/components/MindMapCanvas.tsx` 中：

- 新增 `dragStartRef` 记录 mousedown 时的候选拖拽信息。
- 只有鼠标移动超过 `DRAG_THRESHOLD = 3` 像素，才真正 `setDragging`。
- 普通点击只走 `onSelect`，不走拖拽生命周期。
- 节点始终 `pointerEvents: 'auto'`，不再在拖拽时切为 `none`。
- `handleMouseUp` 中统一清理 `dragging` 和 `dragStartRef`。

## 经验

- 涉及鼠标交互时，应该一开始就把 **click / drag / contextmenu** 三条路径分开，mousedown 不要直接产生副作用。
- 桌面端 WebView 的事件行为和浏览器有差异，不能只看 Chromium 测试结果。
- 对 `pointerEvents: none` 的使用要非常谨慎，它会把后续同一次交互序列的事件目标全部改变。
